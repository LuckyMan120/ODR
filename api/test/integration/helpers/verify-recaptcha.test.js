

describe('verifyRecaptcha', () => {
  describe('verifyRecaptcha', () => {
    it('should expose verifyRecaptcha', () => {
      expect(sails.helpers.verifyRecaptcha).to.be.a('function');
    });
  });
});
